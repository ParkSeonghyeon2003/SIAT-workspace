# [1단계: 패키지 네임스페이스 바인딩]
# 하위 모듈(.calc)에 정의된 기능을 패키지의 루트 네임스페이스(패키지의 가장 최상위 지점)로 끌어올려 등록합니다.
# 이제 외부에서는 'mypackage.multiply'와 'mypackage.sub'로 접근이 가능해집니다.
from .calc import multiply, sub


# [2단계: 공개 네임스페이스 가시성 제어]
# 'from 패키지 import *' 실행 시 사용자의 네임스페이스에 노출될 대상을 명시적으로 제한합니다.
# - "multiply"는 공개 네임스페이스에 포함되어 외부로 노출됩니다.
# - "sub"는 패키지 내부 네임스페이스에는 존재하지마, '*'를 통한 일괄 노출에서는 제외됩니다.
__all__ = ["multiply"]